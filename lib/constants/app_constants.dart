/// Central source of truth for the app brand name.
///
/// Use this constant everywhere the app name is displayed in UI
/// (e.g. as a parameter for localized strings like `l.welcomeTitle(kAppName)`).
const String kAppName = 'Lineup';
